# Public assets

Place **`Krump Verify.png`** here for the Register IP flow. This image is uploaded to IPFS and set as the IPA metadata so your IP shows with the KRUMP VERIFY cover on the Story explorer.

If the file is missing, "Upload metadata to IPFS" will fail with a message about loading the cover image.
